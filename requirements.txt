# Python >= 3.10
numpy>=1.23
pandas>=2.0
scipy>=1.8
scikit-learn>=1.3
torch>=2.1
# The upstream scFormer code this repo builds on imports both of these directly
# (third_party/scFormer/scformer/conv.py), so they are required, not optional.
torch-geometric>=2.6.1
torchmetrics>=0.11.4
scanpy>=1.10
anndata>=0.8
leidenalg>=0.10
python-igraph>=0.11
matplotlib>=3.6
seaborn>=0.13
tqdm>=4.60
pyyaml>=6.0
